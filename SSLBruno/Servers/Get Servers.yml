info:
  name: Get Servers
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/servers
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
