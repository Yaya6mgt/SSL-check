info:
  name: Get Specific Server
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/api/servers/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
