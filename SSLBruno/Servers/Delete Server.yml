info:
  name: Delete Server
  type: http
  seq: 5

http:
  method: DELETE
  url: http://localhost:3000/api/servers/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
