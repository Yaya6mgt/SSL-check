info:
  name: Update Role User
  type: http
  seq: 6

http:
  method: PUT
  url: http://localhost:3000/api/users/2/role
  body:
    type: json
    data: |-
      {
        "role": "editor"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
