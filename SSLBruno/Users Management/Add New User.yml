info:
  name: Add New User
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/users
  body:
    type: json
    data: |-
      {
        "firstName": "Jean",
        "lastName": "Dupond",
        "email": "test2@gmail.com",
        "password": "azerty",
        "role": "editor"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
