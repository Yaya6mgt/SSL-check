info:
  name: Update User
  type: http
  seq: 4

http:
  method: PUT
  url: http://localhost:3000/api/users/2
  body:
    type: json
    data: |-
      {
        "firstName": "Jeanne",
        "lastName": "Duponnne",
        "email": "test2@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
