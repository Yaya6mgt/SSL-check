info:
  name: Update Self
  type: http
  seq: 2

http:
  method: PUT
  url: http://localhost:3000/api/users/me
  body:
    type: json
    data: |-
      {
        "firstName": "Yaya",
        "lastName": "Mimi",
        "email": "test2@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
