info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: http:localhost:3000/api/auth/register
  body:
    type: json
    data: |-
      {
        "firstName": "Yanis",
        "lastName": "Mignot",
        "email": "test1@gmail.com",
        "password": "azerty",
        "role": "admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
