info:
  name: Launch All Check
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/api/domains/check-all
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
