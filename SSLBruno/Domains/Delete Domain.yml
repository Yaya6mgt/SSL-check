info:
  name: Delete Domain
  type: http
  seq: 6

http:
  method: DELETE
  url: http://localhost:3000/api/domains/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
