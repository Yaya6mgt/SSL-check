info:
  name: Check Specific Domain
  type: http
  seq: 5

http:
  method: POST
  url: http://localhost:3000/api/domains/1/check
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
