info:
  name: Get Domains
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/domains
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
