info:
  name: Update Recipient
  type: http
  seq: 3

http:
  method: PUT
  url: http://localhost:3000/api/recipients/1
  body:
    type: json
    data: |-
      {
        "isActive": false
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
