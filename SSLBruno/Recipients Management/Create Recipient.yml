info:
  name: Create Recipient
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/api/recipients
  body:
    type: json
    data: |-
      {
        "email": "yayadeusty@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
